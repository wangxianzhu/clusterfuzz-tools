dependencies:
  override:
    - pip install -U pip
    - pip install -r tool/requirements.txt
    - pip install -e tool/.
test:
  override:
    - python pylint.py
    - cd tool && coverage run test.py
    - cd ci/continuous_integration && coverage run test.py
  post:
    - cd tool && coveralls
