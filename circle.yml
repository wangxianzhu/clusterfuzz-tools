dependencies:
  override:
    - pip install -r tool/requirements.txt
    - pip install -e tool/.
test:
  override:
    - python pylint.py
    - cd tool && coverage run test.py
  post:
    - cd tool && coveralls

